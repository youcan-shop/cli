{
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "lint": "eslint --fix",
    "release": "pnpm build && bumpp packages/*/package.json --commit \"release: v\" --push --tag && pnpm -r release"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
